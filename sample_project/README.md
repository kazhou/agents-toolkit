# sample_project

Test project for experimenting with the agents-toolkit workflow.

## Files

- `main.py` — prints "hello world" 10 times with incrementing exclamation points
- `CLAUDE.md` — project instructions for Claude

## Directories

- `agent_dev/` — workflow docs, drafts, and session logs

## Usage

```bash
uv run python main.py
```
